FROM python:3.11-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends bash libgl1 libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

COPY . /workspace

ENTRYPOINT ["bash", "tools/run_sonar_local.sh"]
